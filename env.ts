import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'ng2-rest-swagger-generator.example.domain.com',
    title: 'Ng 2 Rest Swagger Generator',
  },
};
export default env;
